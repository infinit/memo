network: list-services: Usage: _build/macosx64/bin/infinit-network network list-services [OPTIONS]
List network registered services

Options:
  -h, --help             show this help message
      --compatibility-version  compatibility version to force
  -s, --script           suppress extraneous human friendly messages and use JSON output
  -a, --as               user to run commands as (default: akim)
  -n, --name             name of the network to list
      --peer             peer address or file with list of peer addresses (host:port)
      --async            use asynchronous write operations
      --cache            enable caching with default values
      --cache-ram-size   maximum RAM block cache size in bytes (default: 64 MB)
      --cache-ram-ttl    RAM block cache time-to-live in seconds (default: 5 minute)
      --cache-ram-invalidation  RAM block cache invalidation time in seconds (default: 15 seconds)
      --cache-disk-size  size of disk cache for immutable data in bytes (default: 512 MB)
      --fetch-endpoints  fetch endpoints from https://beyond.infinit.sh
  -f, --fetch            fetch network from https://beyond.infinit.sh
      --push-endpoints   push endpoints to https://beyond.infinit.sh
  -p, --push             push network to https://beyond.infinit.sh
      --publish          alias for --fetch-endpoints --push-endpoints
      --endpoints-file   write node listening endpoints to file (format: host:port)
      --port-file        write node listening port to file
      --port             outbound port to use
      --peers-file       Periodically write list of known peers to given file
      --listen           specify which IP address to listen on (default: all)
      --fetch-endpoints-interval  period for repolling endpoints from the Hub in seconds
      --no-local-endpoints  Disable automatic detection of local endpoints
      --no-public-endpoints  Disable automatic detection of public endpoints
      --advertise-host   advertise extra endpoint using given host

