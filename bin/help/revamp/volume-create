volume: create: Usage: _build/macosx64/bin/infinit-volume volume create [OPTIONS]
Create a volume

Options:
  -h, --help                show this help message
      --compatibility-version arg  compatibility version to force
  -s, --script              suppress extraneous human friendly messages and use JSON output
  -a, --as arg              user to run commands as (default: akim)
  -n, --name arg            name of the volume to create
  -N, --network arg         network to create volume for
      --description arg     volume description
  -R, --create-root         create root directory
      --push-volume         push the volume to https://beyond.infinit.sh
  -o, --output arg          file to write the volume to
  -d, --default-permissions arg  default permissions (optional: r,rw)
  -r, --register-service    register volume in the network
      --allow-root-creation  create the filesystem root if not found
  -m, --mountpoint arg      where to mount the filesystem
      --readonly            mount as readonly
      --mount-name arg      name of mounted volume
      --mount-icon arg      path to an icon for mounted volume
      --finder-sidebar      show volume in Finder sidebar
      --async               use asynchronous write operations
  -d, --daemon              run as a background daemon
      --monitoring          enable monitoring
      --fuse-option arg     option to pass directly to FUSE (default: [])
      --cache               enable caching with default values
      --cache-ram-size arg  maximum RAM block cache size in bytes (default: 64MB)
      --cache-ram-ttl arg   RAM block cache time-to-live in seconds (default: 5min)
      --cache-ram-invalidation arg  RAM block cache invalidation time in seconds (default: 15s)
      --cache-disk-size arg  size of disk cache for immutable data in bytes (default: 512MB)
      --fetch-endpoints     fetch endpoints from https://beyond.infinit.sh
  -f, --fetch               fetch volume from https://beyond.infinit.sh
      --peer arg            peer address or file with list of peer addresses (host:port) (default: [])
      --peers-file arg      Periodically write list of known peers to given file
      --push-endpoints      push endpoints to https://beyond.infinit.sh
  -p, --push                push volume to https://beyond.infinit.sh
      --publish             alias for --fetch-endpoints --push-endpoints
      --advertise-host arg  advertise extra endpoint using given host (default: [])
      --endpoints-file arg  write node listening endpoints to file (format: host:port)
      --port-file arg       write node listening port to file
      --port arg            outbound port to use
      --listen arg          specify which IP address to listen on (default: all)
      --fetch-endpoints-interval arg  period for repolling endpoints from the Hub in seconds (default: 300)
  -i, --input arg           file to read volume from

