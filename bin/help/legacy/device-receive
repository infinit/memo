device: receive: Usage: _build/macosx64/bin/infinit-device --receive [OPTIONS...]

Receive an object from another device using the Hub:

Receive options:
  -n [ --name ] arg      Name of object to receive
  -u [ --user ]          Receive a user identity from another device using the 
                         Hub
  --passphrase arg       Passphrase to secure identity (default: prompt for 
                         passphrase)


