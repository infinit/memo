drive: push: Usage: _build/macosx64/bin/infinit-drive --push --name NAME [OPTIONS...]

Push a drive to the Hub:

Push options:
  -n [ --name ] arg      Drive to push to the Hub
  -i [ --icon ] arg      Path to an image to use as icon


