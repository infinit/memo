Usage: _build/macosx64/bin/infinit-network MODE [OPTIONS...]			Usage: infinit-network network [MODE|--help]
										Infinit network management utility.
Infinit network management utility:
										Modes:
Modes:										  create     Create a network
  --create		       Create a network					  delete     Delete a network locally
  --update		       Update a network					  export     Export a network
  --export		       Export a network					  fetch	     Fetch a network from the Hub
  --fetch		       Fetch a network from the Hub			  import     Fetch a network
  --import		       Import a network					  inspect    Get information about a running network
  --link		       Link this device to a network			  link	     Link this device to a network
  --unlink		       Unlink this device from a network		  list	     List networks
  --list		       List networks					  unlink     Unlink this device from a network
  --push		       Push a network to the Hub			  list-services List network registered services
  --delete		       Delete a network locally				  list-silos List all silos contributed by this device to a network
  --pull		       Remove a network from the Hub			  pull	     Remove a network from the Hub
  --run			       Run a network					  push	     Push a network to the Hub
  --list-services	       List network registered services			  run	     Run a network
  --list-storage	       List all storage contributed by this device to a	  stat	     Fetch stats of a network on the Hub
			       network						  update     Update a network
  --stats		       Fetch stats of a network on the Hub
  --inspect		       Get information about a running network		Options:
										  -h, --help		    show this help message
Miscellaneous:
  -h [ --help ]		       Display the help
  -s [ --script ]	       Silence all extraneous human friendly messages
  --compatibility-version arg  Force compatibility version
  -v [ --version ]	       Display version
  --critical-log	       Path to critical log file, none to disable
  -a [ --as ] arg	       User to run commands as (default: system user)
