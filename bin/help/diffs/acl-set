acl: set: Usage: _build/macosx64/bin/infinit-acl --set --path PATHS [--user    |acl: set: Usage: _build/macosx64/bin/infinit-acl acl set [OPTIONS]
USERS] [OPTIONS...]							       |Set ACLs
									       |
Set ACL:								       |Options:
									       |  -h, --help		    show this help message
Set options:								       |      --compatibility-version arg  compatibility version to force
  -p [ --path ] arg	    Paths					       |  -s, --script		    suppress extraneous human friendly messages and use
  -u [ --user ] arg	    Users and groups (prefix: @<group>)		       |JSON output
  -g [ --group ] arg	    Groups					       |  -a, --as arg		    user to run commands as (default: akim)
  -m [ --mode ] arg	    Access mode: r,w,rw,none			       |      --path arg	    file whose acl to set
  -o [ --others-mode ] arg  Access mode for other network users: r,w,rw,none   |  -u, --user arg	    user to set acl for
  -i [ --enable-inherit ]   New files/folders inherit from their parent	       |  -g, --group arg	    group to set acl for
			    directory					       |  -m, --mode arg	    access mode to set: r, w, rw, none
  --disable-inherit	    New files/folders do not inherit from their parent |  -o, --others-mode arg	    access mode to set for other users: r, w, rw, none
			    directory					       |  -i, --enable-inherit	    make new files and directories inherit permissions
  -R [ --recursive ]	    Apply recursively				       |      --disable-inherit	    make new files and directories not inherit
  --traverse		    Add read permissions to parent directories	       |permissions
  --verbose		    Verbose output				       |  -R, --recursive	    set acl recursively
  --fetch		    Fetch users from the Hub if needed		       |  -t, --traverse	    set read permission on parent directories
									       |  -v, --verbose		    use verbose output
Hidden options:								       |  -f, --fetch		    fetch acl from https://beyond.infinit.sh
  --fallback-xattrs	    Fallback to alternate xattr mode if system xattrs  |      --fallback-xattrs	    use fallback special file if extended attributes
			    are not suppported				       |are not supported
									       |
									       |
