network: create: Usage: _build/macosx64/bin/infinit-network --create --name    |network: create: Usage: _build/macosx64/bin/infinit-network network create
NAME [OVERLAY-TYPE OVERLAY-OPTIONS...] [CONSENSUS-TYPE CONSENSUS-OPTIONS...]   |[OPTIONS]
[--storage STORAGE...] [OPTIONS...]					       |Create a network
									       |
Create a network:							       |Options:
									       |  -h, --help		    show this help message
Create options:								       |      --compatibility-version arg  compatibility version to force
  -n [ --name ] arg		   Created network name			       |  -s, --script		    suppress extraneous human friendly messages and use
  --description arg		   Description of network		       |JSON output
  -S [ --storage ] arg		   Storage to contribute (optional, data       |  -a, --as arg		    user to run commands as (default: akim)
				   striped over multiple)		       |  -n, --name arg	    name of the network to create
  --port arg			   Port to listen on (default: random)	       |      --description arg	    network description
  -r [ --replication-factor ] arg  Data replication factor (default: 1)	       |  -S, --storage arg	    storage to contribute (optional, data striped over
  -e [ --eviction-delay ] arg	   Missing servers eviction delay	       |multiple) (default: [])
				   (default: 10 min)			       |      --port arg	    outbound port to use
  -o [ --output ] arg		   File to write the network to		       |  -r, --replication-factor arg	data replication factor (default: 1) (default:
				   (default: stdout)			       |1)
  --push-network		   Push the network to the Hub		       |  -e, --eviction-delay arg  missing servers eviction delay (default: 10min)
  -p [ --push ]			   Alias for --push-network		       |  -o, --output arg	    file to write the network to
  --admin-r arg			   Set admin users that can read all data      |      --push-network	    push the network to https://beyond.infinit.sh
  --admin-rw arg		   Set admin users that can read and write all |  -p, --push		    push network to https://beyond.infinit.sh
				   data					       |      --admin-r arg	    Set admin users that can read all data (default: []
  --peer arg			   List of known node endpoints		       |      --admin-rw arg	    Set admin users that can read and write all data
									       |(default: [])
Consensus types:							       |      --peer arg	    peer address or file with list of peer addresses
  --paxos			   Use Paxos consensus algorithm (default)     |(host:port) (default: [])
  --no-consensus		   Use no consensus algorithm		       |      --paxos		    use Paxos consensus algorithm (default)
									       |      --no-consensus	    use no consensus algorithm
Overlay types:								       |      --kelips		    use a Kelips overlay network (default)
  --kelips			   Use a Kelips overlay network (default)      |      --kalimero	    use a Kalimero overlay network. Used for local
  --kalimero			   Use a Kalimero overlay network.	       |testing
				   Used for local testing		       |      --kouncil		    use a Kouncil overlay network
  --kouncil			   Use a Kouncil overlay network	       |      --nodes arg	    estimate of the total number of nodes
									       |      --k arg		    number of groups (default: 1)
Kelips options:								       |      --kelips-contact-timeout arg  ping timeout before considering a peer lost
  --nodes arg			   Estimate of the total number of nodes       |(default: 2min)
  --k arg			   Number of groups (default: 1)	       |      --encrypt arg	    use encryption: no, lazy, yes (default: yes)
  --kelips-contact-timeout arg	   Ping timeout before considering a peer lost |      --protocol arg	    RPC protocol to use: tcp, utp, all (default: all)
				   (default: 2 min)			       |
  --encrypt arg			   Use encryption: no,lazy,yes (default: yes)  |
  --protocol arg		   RPC protocol to use: tcp,utp,all	       |
				   (default: all)			       |
									       |
									       |
