network: create: Usage: _build/macosx64/bin/infinit-network --create --name    |network: create: Usage: _build/macosx64/bin/infinit-network network create
NAME [OVERLAY-TYPE OVERLAY-OPTIONS...] [CONSENSUS-TYPE CONSENSUS-OPTIONS...]   |[OPTIONS]
[--storage STORAGE...] [OPTIONS...]					       |Create a network
									       |
Create a network:							       |Options:
									       |  -h, --help		 show this help message
Create options:								       |      --compatibility-version  compatibility version to force
  -n [ --name ] arg		   Created network name			       |  -s, --script		 suppress extraneous human friendly messages and use
  --description arg		   Description of network		       |JSON output
  -S [ --storage ] arg		   Storage to contribute (optional, data       |  -a, --as		 user to run commands as (default: akim)
				   striped over multiple)		       |  -n, --name		 name of the network to create
  --port arg			   Port to listen on (default: random)	       |      --description	 network description
  -r [ --replication-factor ] arg  Data replication factor (default: 1)	       |  -S, --storage		 storage to contribute (optional, data striped over
  -e [ --eviction-delay ] arg	   Missing servers eviction delay	       |multiple) (default: [])
				   (default: 10 min)			       |      --port		 outbound port to use
  -o [ --output ] arg		   File to write the network to		       |  -r, --replication-factor  data replication factor (default: 1) (default: 1)
				   (default: stdout)			       |  -e, --eviction-delay	 missing servers eviction delay (default: 10 min)
  --push-network		   Push the network to the Hub		       |  -o, --output		 file to write the network to
  -p [ --push ]			   Alias for --push-network		       |      --push-network	 push the network to https://beyond.infinit.sh
  --admin-r arg			   Set admin users that can read all data      |  -p, --push		 push network to https://beyond.infinit.sh
  --admin-rw arg		   Set admin users that can read and write all |      --admin-r		 Set admin users that can read all data (default: [])
				   data					       |      --admin-rw	 Set admin users that can read and write all data
  --peer arg			   List of known node endpoints		       |(default: [])
									       |      --peer		 peer address or file with list of peer addresses
Consensus types:							       |(host:port) (default: [])
  --paxos			   Use Paxos consensus algorithm (default)     |      --paxos		 use Paxos consensus algorithm (default)
  --no-consensus		   Use no consensus algorithm		       |      --no-consensus	 use no consensus algorithm
									       |      --kelips		 use a Kelips overlay network (default)
Overlay types:								       |      --kalimero	 use a Kalimero overlay network. Used for local testing
  --kelips			   Use a Kelips overlay network (default)      |      --kouncil		 use a Kouncil overlay network
  --kalimero			   Use a Kalimero overlay network.	       |      --nodes		 estimate of the total number of nodes
				   Used for local testing		       |      --k		 number of groups (default: 1)
  --kouncil			   Use a Kouncil overlay network	       |      --kelips-contact-timeout	ping timeout before considering a peer lost
									       |(default: 2min)
Kelips options:								       |      --encrypt		 use encryption: no, lazy, yes (default: yes)
  --nodes arg			   Estimate of the total number of nodes       |      --protocol	 RPC protocol to use: tcp, utp, all (default: all)
  --k arg			   Number of groups (default: 1)	       |
  --kelips-contact-timeout arg	   Ping timeout before considering a peer lost |
				   (default: 2 min)			       |
  --encrypt arg			   Use encryption: no,lazy,yes (default: yes)  |
  --protocol arg		   RPC protocol to use: tcp,utp,all	       |
				   (default: all)			       |
									       |
									       |
