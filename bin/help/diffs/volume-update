volume: update: Usage: _build/macosx64/bin/infinit-volume --update --name      |volume: update: Usage: _build/macosx64/bin/infinit-volume volume update
VOLUME [OPTIONS...]							       |[OPTIONS]
									       |Update a volume with default run options
Update a volume with default run options:				       |
									       |Options:
Update options:								       |  -h, --help		 show this help message
  --name arg				Volume name			       |      --compatibility-version  compatibility version to force
  --description arg			Description of volume		       |  -s, --script		 suppress extraneous human friendly messages and use
  --allow-root-creation [=arg(=true)]	Create the filesystem root if not found|JSON output
  -m [ --mountpoint ] arg		Where to mount the filesystem	       |  -a, --as		 user to run commands as (default: akim)
  --readonly [=arg(=true)]		Mount as readonly		       |  -n, --name		 name of the volume to update
  --mount-name arg			Name of mounted volume		       |      --description	 volume description
  --mount-icon arg			Path to an icon for mounted volume     |      --allow-root-creation  create the filesystem root if not found
  --finder-sidebar [=arg(=true)]	Show volume in Finder sidebar	       |  -m, --mountpoint	 where to mount the filesystem
  --async [=arg(=true)]			Use asynchronous write operations      |      --readonly	 mount as readonly
  -d [ --daemon ] [=arg(=true)]		Run as a background daemon	       |      --mount-name	 name of mounted volume
  --monitoring [=arg(=true)] (=true)	Enable monitoring (default: true)      |      --mount-icon	 path to an icon for mounted volume
  --fuse-option arg			Option to pass directly to FUSE	       |      --finder-sidebar	 show volume in Finder sidebar
  --cache [=arg(=true)]			Enable caching with default values     |      --async		 use asynchronous write operations
  --cache-ram-size arg			Maximum RAM block cache size in bytes  |  -d, --daemon		 run as a background daemon
					(default: 64 MB)		       |      --monitoring	 enable monitoring
  --cache-ram-ttl arg			RAM block cache time-to-live in seconds|      --fuse-option	 option to pass directly to FUSE (default: [])
					(default: 5 minutes)		       |      --cache		 enable caching with default values
  --cache-ram-invalidation arg		RAM block cache invalidation time in   |      --cache-ram-size	 maximum RAM block cache size in bytes (default: 64 MB)
					seconds (default: 15 seconds)	       |      --cache-ram-ttl	 RAM block cache time-to-live in seconds (default: 5
  --cache-disk-size arg			Size of disk cache for immutable data  |minute)
					in bytes (default: 512 MB)	       |      --cache-ram-invalidation	RAM block cache invalidation time in seconds
  --fetch-endpoints [=arg(=true)]	Fetch endpoints from the Hub	       |(default: 15 seconds)
  -f [ --fetch ] [=arg(=true)]		Alias for --fetch-endpoints	       |      --cache-disk-size	 size of disk cache for immutable data in bytes
  --peer arg				Peer address or file with list of peer |(default: 512 MB)
					addresses (host:port)		       |      --fetch-endpoints	 fetch endpoints from https://beyond.infinit.sh
  --peers-file arg			Periodically write list of known peers |  -f, --fetch		 fetch volume from https://beyond.infinit.sh
					to given file			       |      --peer		 peer address or file with list of peer addresses
  --push-endpoints [=arg(=true)]	Push endpoints to the Hub	       |(host:port) (default: [])
  -p [ --push ] [=arg(=true)]		Alias for --push-endpoints	       |      --peers-file	 Periodically write list of known peers to given file
  --map-other-permissions [=arg(=true)] (=true)				       |      --push-endpoints	 push endpoints to https://beyond.infinit.sh
					Allow chmod to set world permissions   |  -p, --push		 push volume to https://beyond.infinit.sh
  --publish [=arg(=true)]		Alias for --fetch-endpoints	       |      --map-other-permissions  allow chmod to set world permissions
					--push-endpoints		       |      --publish		 alias for --fetch-endpoints --push-endpoints
  --advertise-host arg			Advertise extra endpoint using given   |      --advertise-host	 advertise extra endpoint using given host (default: []
					host				       |      --endpoints-file	 write node listening endpoints to file (format:
  --endpoints-file arg			Write node listening endpoints to file |host:port)
					(format: host:port\n)		       |      --port-file	 write node listening port to file
  --port-file arg			Write node listening port to file      |      --port		 outbound port to use
  --port arg				Outbound port to use		       |      --listen		 specify which IP address to listen on (default: all)
  --listen arg				Specify which IP address to listen on  |      --fetch-endpoints-interval  period for repolling endpoints from the Hub
					(default: all)			       |in seconds (default: 300)
  --fetch-endpoints-interval arg (=300) Period for repolling endpoints from the|  -i, --input		 file to read volume from
					Hub in seconds (default: 300s)	       |  -u, --user		 user to update volume for
  -i [ --input ] arg			File to read the commands from	       |
					(default: stdin)		       |
  -u [ --user ] arg			Force mounting user to USER	       |
									       |
Deprecated options:							       |
  --cache-size arg			Use "--cache-ram-size" instead	       |
  --cache-ttl arg			Use "--cache-ram-ttl" instead	       |
  --cache-invalidation arg		Use "--cache-ram-invalidation" instead |
  --disk-cache-size arg			Use "--cache-disk-size" instead	       |
									       |
									       |
