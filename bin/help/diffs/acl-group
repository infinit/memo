acl: group: Usage: _build/macosx64/bin/infinit-acl --group --name NAME --path  |acl: group: Usage: _build/macosx64/bin/infinit-acl acl group [OPTIONS]
PATH [OPTIONS...]							       |Edit groups
									       |
Group control:								       |Options:
									       |  -h, --help		    show this help message
Group options:								       |      --compatibility-version arg  compatibility version to force
  -n [ --name ] arg	 Group name					       |  -s, --script		    suppress extraneous human friendly messages and use
  -c [ --create ]	 Create the group				       |JSON output
  --description arg	 Description of group				       |  -a, --as arg		    user to run commands as (default: akim)
  --show		 List group users, administrators and description      |      --path arg	    file whose acl to group
  --add-user arg	 Add user to group				       |  -n, --name arg	    name of the acl to group
  --add-admin arg	 Add administrator to group			       |  -c, --create		    create the acl
  --add-group arg	 Add group to group				       |      --delete		    delete the acl
  --add arg		 Add users, administrators and groups to group (prefix:|      --show		    list group users, administrators and description
			 @<group>, ^<admin>)				       |      --description arg	    acl description
  --remove-user arg	 Remove user from group				       |      --add-user arg	    add user to group (default: [])
  --remove-admin arg	 Remove administrator from group		       |      --add-group arg	    add group to group (default: [])
  --remove-group arg	 Remove group from group			       |      --add-admin arg	    add administrator to group (default: [])
  --remove arg		 Remove users, administrators and groups from group    |      --add arg		    add users, administrators and groups to group
			 (prefix: @<group>, ^<admin>)			       |(prefix: @<group>, ^<admin> (default: [])
  -p [ --path ] arg	 A path within the volume			       |      --remove-user arg	    remove user from group (default: [])
  --verbose		 Verbose output					       |      --remove-group arg    remove group from group (default: [])
  --fetch		 Fetch users from the Hub if needed		       |      --remove-admin arg    remove administrator from group (default: [])
									       |      --remove arg	    remove users, administrators and groups from group
Hidden options:								       |(prefix: @<group>, ^<admin> (default: [])
  --fallback-xattrs	 Fallback to alternate xattr mode if system xattrs are |  -v, --verbose		    use verbose output
			 not suppported					       |  -f, --fetch		    fetch acl from https://beyond.infinit.sh
									       |      --fallback-xattrs	    use fallback special file if extended attributes
									       |are not supported
									       |
