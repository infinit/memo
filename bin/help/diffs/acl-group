acl: group: Usage: _build/macosx64/bin/infinit-acl --group --name NAME --path P acl: group: Usage: infinit acl group [OPTIONS]
										Edit groups
Group control:
										Options:
Group options:									  -h, --help		    show this help message
  -n [ --name ] arg	 Group name						      --compatibility-version arg  compatibility version to force
  -c [ --create ]	 Create the group					  -s, --script		    suppress extraneous human friendly messages and use
  --description arg	 Description of group					  -a, --as arg		    user to run commands as (default: john-doe)
  --show		 List group users, administrators and description	      --path arg	    file whose acl to group
  --add-user arg	 Add user to group					  -n, --name arg	    name of the acl to group
  --add-admin arg	 Add administrator to group				  -c, --create		    create the acl
  --add-group arg	 Add group to group					      --delete		    delete the acl
  --add arg		 Add users, administrators and groups to group (prefix:	      --show		    list group users, administrators and description
			 @<group>, ^<admin>)					      --description arg	    acl description (default: null)
  --remove-user arg	 Remove user from group					      --add-user arg	    add user to group (default: [])
  --remove-admin arg	 Remove administrator from group			      --add-group arg	    add group to group (default: [])
  --remove-group arg	 Remove group from group				      --add-admin arg	    add administrator to group (default: [])
  --remove arg		 Remove users, administrators and groups from group	      --add arg		    add users, administrators and groups to group (pref
			 (prefix: @<group>, ^<admin>)				      --remove-user arg	    remove user from group (default: [])
  -p [ --path ] arg	 A path within the volume				      --remove-group arg    remove group from group (default: [])
  --verbose		 Verbose output						      --remove-admin arg    remove administrator from group (default: [])
  --fetch		 Fetch users from the Hub if needed			      --remove arg	    remove users, administrators and groups from group
										  -v, --verbose		    use verbose output
Hidden options:									  -f, --fetch		    fetch acl from the Hub
  --fallback-xattrs	 Fallback to alternate xattr mode if system xattrs are	      --fallback-xattrs	    use fallback special file if extended attributes ar
			 not suppported
