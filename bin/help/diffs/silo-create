storage: create: Usage: _build/macosx64/bin/infinit-storage --create --name    |silo: create: Usage: _build/macosx64/bin/infinit-silo silo create [OPTIONS]
STORAGE STORAGE-TYPE [STORAGE-OPTIONS...] [OPTIONS...]			       |Create local silo
									       |
Create a storage:							       |Options:
									       |  -h, --help		    show this help message
Create options:								       |      --compatibility-version arg  compatibility version to force
  -n [ --name ] arg	 Created storage name				       |  -s, --script		    suppress extraneous human friendly messages and use
  --description arg	 Description of storage				       |JSON output
  -c [ --capacity ] arg	 Limit the storage capacity			       |  -a, --as arg		    user to run commands as (default: akim)
			 use: B,kB,kiB,GB,GiB,TB,TiB (optional)		       |  -n, --name arg	    name of the silo to create
  -o [ --output ] arg	 File to write the storage to			       |      --description arg	    silo description
			 (default: stdout)				       |  -c, --capacity arg	    limit storage capacity (use:
  --path arg		 Folder where blocks are stored (default:	       |B,kB,kiB,MB,MiB,GB,GiB,TB,TiB)
			   filesystem: "/Users/akim/.local/share/infinit/filesy|  -o, --output arg	    file to write the silo to
			 stem/blocks/<name>"				       |      --dropbox arg	    store blocks on Dropbox
			   GCS: <name>_blocks				       |      --filesystem arg	    store blocks on local filesystem (default)
			   S3: <name>_blocks)				       |      --gcs arg		    store blocks on Google Cloud Storage
									       |      --google-drive arg    store blocks on Google Drive
Storage types:								       |      --ssh arg		    store blocks via SSH
  --filesystem		 Store data on a local filesystem (default)	       |      --s3 arg		    store blocks on AWS S3
  --gcs			 Store data using Google Cloud Storage		       |      --account arg	    cloud service account name
  --s3			 Store data using Amazon S3			       |      --bucket arg	    bucket name
									       |      --endpoint arg	    S3 endpoint (default: amazonaws.com)
Cloud storage options:							       |      --host arg	    SSH host
  --account arg		 Account name to use				       |      --region arg	    AWS region
  --bucket arg		 Bucket to use					       |      --path arg	    directory where to store blocks
									       |      --storage-class arg   storage class to use: STANDARD, STANDARD_IA,
Amazon S3 storage options:						       |REDUCED_REDUNDANCY (default: bucket default)
  --storage-class arg	 Storage class to use: STANDARD, STANDARD_IA,	       |
			 REDUCED_REDUNDANCY (default: bucket default)	       |
  --region arg		 AWS region to use				       |
  --endpoint arg	 S3 compatible endpoint to use (default: amazonaws.com)|
									       |
Hidden storage types:							       |
  --dropbox		 Store data in a Dropbox			       |
  --google-drive	 Store data in a Google Drive			       |
  --ssh			 Store data over SSH				       |
									       |
									       |
