daemon: run: Usage: _build/macosx64/bin/infinit-daemon --run [OPTIONS...]      |daemon: run: Usage: _build/macosx64/bin/infinit-daemon daemon run [OPTIONS]
									       |Run daemon in the foreground
Run daemon in the foreground:						       |
									       |Options:
Run options:								       |  -h, --help		 show this help message
  --login-user arg		  Login with selected user(s), of form	       |      --compatibility-version  compatibility version to force
				  'user:password'			       |  -s, --script		 suppress extraneous human friendly messages and use
  --mount-root arg		  Default root path for all mounts	       |JSON output
				  (default: /run/user/505/infinit/filesystem/mn|  -a, --as		 user to run commands as (default: akim)
				  t)					       |      --login-user	 Login with selected user(s), of form 'user:password'
  --default-network arg		  Default network for volume creation	       |(default: [])
  --advertise-host arg		  Advertise given hostname as an extra endpoint|      --mount		 mount given volumes on startup, keep trying on error
				  when running volumes			       |(default: [])
  -f [ --fetch ]		  Run volumes with --fetch		       |      --mount-root	 Default root path for all mounts
  -p [ --push ]			  Run volumes with --push		       |      --default-network	 Default network for volume creation
  --publish			  Alias for --publish			       |      --advertise-host	 advertise extra endpoint using given host (default: []
  --docker [=arg(=true)] (=true)  Enable the Docker plugin		       |  -f, --fetch		 fetch daemon from https://beyond.infinit.sh
				  (default: true)			       |  -p, --push		 push daemon to https://beyond.infinit.sh
  --docker-user arg		  System user to use for docker plugin	       |      --docker		 Enable the Docker plugin
				  (default: akim)			       |      --docker-user	 System user to use for docker plugin
  --docker-home arg		  Home directory to use for Docker user	       |      --docker-home	 Home directory to use for Docker user (default:
				  (default: /home/<docker-user>)	       |/home/<docker-user>)
  --docker-socket-tcp		  Use a TCP socket for docker plugin	       |      --docker-socket-tcp  Use a TCP socket for docker plugin
  --docker-socket-port arg	  TCP port to use to communicate with Docker   |      --docker-socket-port  TCP port to use to communicate with Docker, 0 for
				  (default: random)			       |random (default: 0)
  --docker-socket-path arg	  Path for plugin socket		       |      --docker-socket-path  Path for plugin socket (default:
				  (default: /run/docker/plugins)	       |/run/docker/plugins)
  --docker-descriptor-path arg	  Path to add plugin descriptor		       |      --docker-descriptor-path	Path to add plugin descriptor (default:
				  (default: /usr/lib/docker/plugins)	       |/usr/lib/docker/plugins)
  --docker-mount-substitute arg	  [from:to|prefix] : Substitute 'from' to 'to' |      --docker-mount-substitute	 [from:to|prefix] : Substitute 'from' to 'to'
				  in advertised path			       |in advertised path (default: )
  -l [ --log-level ] arg	  Log level to start volumes with (default:    |      --log-level	 Log level to start volumes with (default: LOG)
				  LOG)					       |      --log-path	 Store volume logs in given path
  -d [ --log-path ] arg		  Store volume logs in given path	       |
									       |
									       |
