Usage: _build/macosx64/bin/infinit-credentials MODE [OPTIONS...]		Usage: infinit credentials [MODE|--help]
										Infinit credentials management utility.
Infinit third-party credentials utility:
										Modes:
Modes:										  add	     Add credentials for a third-party service
  --add			       Add credentials for a third-party service	  delete     Delete locally credentials for a third-party service
  --fetch		       Fetch credentials from the Hub			  fetch	     Fetch credentials from the Hub
  --list		       List local credentials				  pull	     Pull credentials from the Hub
  --delete		       Delete credentials locally			  list	     List local credentials

Miscellaneous:									Options:
  -h [ --help ]		       Display the help					  -h, --help		    show this help message
  -s [ --script ]	       Silence all extraneous human friendly messages
  --compatibility-version arg  Force compatibility version
  -v [ --version ]	       Display version
  --critical-log	       Path to critical log file, none to disable
  -a [ --as ] arg	       User to run commands as (default: system user)
