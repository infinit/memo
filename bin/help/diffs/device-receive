device: receive: Usage: _build/macosx64/bin/infinit-device --receive [OPTIONS.. device: receive: Usage: _build/macosx64/bin/infinit-device device receive [OPTI
										receive an object from another device using {hub}
Receive an object from another device using the Hub:
										Options:
Receive options:								  -h, --help		 show this help message
  -n [ --name ] arg	 Name of object to receive				      --compatibility-version  compatibility version to force
  -u [ --user ]		 Receive a user identity from another device using the	  -s, --script		 suppress extraneous human friendly messages and use JS
			 Hub							  -a, --as		 user to run commands as (default: akim)
  --passphrase arg	 Passphrase to secure identity (default: prompt for	  -u, --user		 to receive user identity to another device using https
			 passphrase)						  -n, --name		 name of the device to receive (default: akim)
										      --passphrase	 passphrase to secure identity (default: prompt for pas

